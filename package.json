{
  "name": "meal-planner",
  "version": "1.0.0",
  "scripts": {
    "deploy-api": "npx sls api:deploy"
  },
  "private": true,
  "workspaces": [
    "packages/api",
    "packages/web-client"
  ],
  "devDependencies": {
    "@serverless/compose": "1.3.0"
  }
}
